name: CI Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout del código
        uses: actions/checkout@v4

      - name: 🔧 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 🧪 Ejecutar tests
        run: npm test --if-present

      - name: 🛠️ Verificar formato y lint (si existen)
        run: |
          npm run lint --if-present
          npm run format --if-present

      - name: 📁 Crear artefacto (build)
        run: npm run build --if-present

      - name: ⬆️ Subir artefacto generado
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact
          path: |
            dist
            build
            ./
